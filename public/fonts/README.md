# Fonty ve `/public/fonts`

Skriptové písmo v hero sekci (`font-script`) je **Momo Signature** z Google Fonts — načítá se v `globals.css` přes `@import`, **není potřeba** sem nic vkládat.

Do této složky můžeš přidat další vlastní fonty (např. pro experimenty); web je jinak nepotřebuje.
