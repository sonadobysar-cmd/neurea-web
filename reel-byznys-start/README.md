# Reel — Byznys Start Program (1080×1920, 28 s, 24 fps)

Kinetic text reel pro NIADO BY SAR / Byznys Start. HTML/CSS → Playwright → ffmpeg.

## Použití

```bash
cd reel-byznys-start
npm install
npx playwright install chromium
npm run build
```

Výstup: `reel.mp4` + `qa/` snímky.

## Timeline (28 s)

| Čas | Scéna |
|-----|-------|
| 0–3,2 s | Hook — Byznys Start Program |
| 3,2–7,5 s | Pro koho (směr / strach) |
| 7,5–14 s | Od A do Z |
| 14–18,5 s | 1. hodina zdarma |
| 18,5–24 s | Founding: 2. hodina za 990 Kč |
| 24–28 s | CTA — napiš START |
